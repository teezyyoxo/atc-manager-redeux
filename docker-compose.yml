services:
  web:
    image: "atc-manager:${APP_VERSION:-3.0.0-rc.23}"
    build:
      context: .
      args:
        APP_VERSION: "${APP_VERSION:-3.0.0-rc.23}"
        BUILD_COMMIT: "${BUILD_COMMIT:-unknown}"
        PREACT_APP_MOBILE_TRAFFIC_FACTOR: "${PREACT_APP_MOBILE_TRAFFIC_FACTOR:-0.5}"
    ports:
      - "${PORT:-8080}:80"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - wget
        - --quiet
        - --tries=1
        - --spider
        - http://127.0.0.1/healthz
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
