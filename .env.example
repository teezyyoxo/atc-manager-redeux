# Preferred host port. Make-based deployment finds a nearby free port if needed.
PORT=8080

# Scales initial aircraft and spawn frequency on mobile/tablet sessions only.
# Use a value greater than 0 and no greater than 1. Desktop always remains 1x.
PREACT_APP_MOBILE_TRAFFIC_FACTOR=0.5
